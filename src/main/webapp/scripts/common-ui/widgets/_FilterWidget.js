define([
    "common-ui/widgets/_Widget"
], function(_Widget) {

    "use strict";

    return _Widget.extend({

        getSelectedLabel : function() {
            return "";
        }
    });
});